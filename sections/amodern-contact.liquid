<style>
  :root {
    --orange: #ffa800;
    --orange-dark: #ff8a00;
    --accent: #ff6a3d;
    --white: #fff;
    --black: #111;
    --radius: 24px;
  }

  .contact-section {
    background: var(--orange);
    padding: 100px 20px;
    font-family: 'Inter', system-ui, -apple-system, sans-serif;
    min-height: 90vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .contact-container {
    max-width: 1100px;
    width: 100%;
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: 60px;
    background: var(--orange-dark);
    padding: 60px;
    border-radius: 50px;
    box-shadow: 0 30px 60px rgba(0,0,0,0.15);
  }

  /* SOL TARAF - BİLGİLER */
  .contact-info {
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .contact-info h2 {
    color: var(--white);
    font-size: clamp(32px, 5vw, 52px);
    font-weight: 900;
    margin-bottom: 24px;
    line-height: 1.1;
    letter-spacing: -1px;
  }

  .contact-info p {
    color: rgba(255,255,255,0.95);
    font-size: 18px;
    margin-bottom: 48px;
    line-height: 1.6;
  }

  .info-item {
    display: flex;
    align-items: center;
    gap: 20px;
    margin-bottom: 30px;
    color: var(--white);
    font-weight: 500;
  }

  .info-icon {
    width: 50px;
    height: 50px;
    background: rgba(255,255,255,0.2);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 22px;
    backdrop-filter: blur(5px);
  }

  /* SAĞ TARAF - FORM */
  .contact-form-card {
    background: var(--white);
    padding: 50px;
    border-radius: 32px;
    box-shadow: 0 20px 40px rgba(0,0,0,0.05);
  }

  /* Mesaj Başarı Kutusu */
  .form-success {
    background: #e8f5e9;
    color: #2e7d32;
    padding: 20px;
    border-radius: 14px;
    margin-bottom: 25px;
    font-weight: 600;
    text-align: center;
    border: 1px solid #c8e6c9;
  }

  .contact-form-card input,
  .contact-form-card textarea {
    width: 100%;
    padding: 18px 24px;
    margin-bottom: 20px;
    border: 2px solid #f2f2f2;
    border-radius: 16px;
    outline: none;
    transition: all .3s ease;
    font-family: inherit;
    box-sizing: border-box;
    font-size: 16px;
  }

  .contact-form-card input:focus,
  .contact-form-card textarea:focus {
    border-color: var(--orange);
    background: #fffdfa;
    box-shadow: 0 0 0 4px rgba(255, 168, 0, 0.1);
  }

  .send-btn {
    width: 100%;
    padding: 20px;
    background: var(--black); /* Daha premium bir kontrast için siyah yaptım kankam */
    color: white;
    border: none;
    border-radius: 16px;
    font-size: 16px;
    font-weight: 700;
    cursor: pointer;
    text-transform: uppercase;
    letter-spacing: 1px;
    transition: all .4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .send-btn:hover {
    transform: translateY(-5px);
    background: var(--accent);
    box-shadow: 0 15px 30px rgba(255,106,61,0.3);
  }

  /* =========================
     CONTACT RESPONSIVE
     ========================= */
  @media (max-width: 990px) {
    .contact-section { padding: 60px 20px; }
    .contact-container {
      grid-template-columns: 1fr;
      padding: 40px;
      gap: 50px;
      border-radius: 40px;
    }
    .contact-info { text-align: center; align-items: center; }
    .contact-info p { margin-bottom: 30px; }
    .contact-form-card { padding: 30px; }
  }

  @media (max-width: 480px) {
    .contact-container { padding: 30px 20px; }
    .contact-info h2 { font-size: 36px; }
    .contact-form-card input, .contact-form-card textarea { padding: 15px; }
    .send-btn { padding: 16px; font-size: 14px; }
  }
</style>

<section class="contact-section">
  <div class="contact-container">
    <div class="contact-info">
      <h2>Let's Talk! 🍊</h2>
      <p>
        Have a question or just want to say hi? We're here to help you 24/7. Our team usually responds within 2 hours.
      </p>

      <div class="info-item">
        <div class="info-icon">📍</div>
        <span>123 Orange Street, Design City</span>
      </div>

      <div class="info-item">
        <div class="info-icon">📧</div>
        <span>support@yourstore.com</span>
      </div>

      <div class="info-item">
        <div class="info-icon">📞</div>
        <span>+1 (555) 000-ORANGE</span>
      </div>
    </div>

    <div class="contact-form-card">
      {% form 'contact' %}
        {% if form.posted_successfully? %}
          <div class="form-success">Kankam mesajın ulaştı! Ekibimiz hemen incelemeye alıyor. ✨</div>
        {% endif %}

        {% if form.errors %}
          <div style="color: #d32f2f; margin-bottom: 20px;">
            Hoppala! Formda bir hata var, bilgileri kontrol eder misin?
          </div>
        {% endif %}

        <input type="text" name="contact[name]" placeholder="Full Name" required>
        <input type="email" name="contact[email]" placeholder="Email Address" required>
        <textarea name="contact[body]" rows="4" placeholder="How can we help you today?" required></textarea>

        <button type="submit" class="send-btn">Send Message</button>
      {% endform %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Modern Contact Orange",
  "settings": [],
  "presets": [{ "name": "Modern Contact Orange" }]
}
{% endschema %}
